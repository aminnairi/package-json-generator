name: Elm CI

on:
    push:
        branch:
            - latest

    pull_request:
        branch:
            - latest

jobs:
    elm-format:
        runs-on: ubuntu-latest
        name: Elm Format
        uses: actions/checkout@v2

        steps:
            - name: Elm Syntax Validation
              run: make format
